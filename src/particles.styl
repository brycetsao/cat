#particles
  & > canvas
    position absolute
    top 0
    left 0
    height 100%
    width 100%
    overflow hidden
    z-index -1
    filter blur(1px)
    &.glow
      z-index -2
      filter blur(8px) saturate(150%) brightness(150%)